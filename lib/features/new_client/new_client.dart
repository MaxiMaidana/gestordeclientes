import 'package:flutter/material.dart';

class NewClient extends StatelessWidget {
  const NewClient({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('new client');
  }
}
